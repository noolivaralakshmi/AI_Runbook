# Incident Fix

## fix: Login App 500 Error Due to Missing Table

## Automated Incident Fix

Resolves #11

**Root Cause:** The 'user_credentials' table is missing from the SQLite database, likely due to a failure in database initialization, migration, or incorrect database path configuration following a recent deployment or code change.

**Suggested Fix:** The application is failing because it's trying to access a 'user_credentials' table that doesn't exist in the SQLite database. The actual table name in the codebase is 'users'. We need to correct the table name in the database query at line 62 of app.py.

---
*Generated by Runbook AI Pipeline*

## Files to investigate
- `login_app/app.py`
